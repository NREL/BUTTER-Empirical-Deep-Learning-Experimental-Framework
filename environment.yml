name: dmp
dependencies:
- python >= 3.10
- pandas
- scikit-learn
- pip
- scipy
- numpy
- conda-forge::tensorflow >= 2.7
- simplejson
- libpq
- bzip2
- lz4
- pip:
  - pyarrow >= 10
  - psycopg[binary] >= 3
  - psycopg_pool >= 3
  - pytest
  - tensorflow_datasets
  - pmlb
  - pathos


  
